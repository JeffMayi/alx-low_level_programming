#include "main.h"
/**
 * print_most_numbers - print_most_numbers
 * followed by a new line.
 * Return: void
 */
void print_most_numbers(void)
{
	int i;

	for (i = 0; i < 10; i++)
	{
	if (1 != 2 && i != 4)
	}
	putchar(i + '0');
	{
	putchar('\n');
	}
}
