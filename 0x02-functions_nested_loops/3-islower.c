#include "main.h"
/**
 * _islower (int c)- a function that checks for lowercase _islower
 * Return: 1 for lowercase character or 0 for otherwise
 */
int main(void)
{
int _islower(int c)
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);
}
