#include "main.h"
/**
 * char _strcpy - copies the string pointed to by src
 * @dest:copy to
 * @src: copy to
 * Return: string
 */
char *_strcpy(char *dest, char *src)
{
	int 1 = 0;
	int 2 = 0;
	while (*(src + 1) != '\0')
	{
		1++;
	}
	for (; x < 1; x++)
	{
		dest[x] = src[x];
		return (dest);
	}
	dest[1] = '\0';
	return (dest);
}
