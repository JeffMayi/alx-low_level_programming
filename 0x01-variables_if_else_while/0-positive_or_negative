#include <stdlib.h>
#include <time.h>
/* more headers goes there */
/**
*main - program that prints negative, zero or positive
*Return: always 0
*/
include <stdio.h>
/* betty style doc for function main goes there */
int main(void)
{
	int n;

	srand(time(0));
	n = rand() - RAND_MAX / 2;
	/* your code goes there */
	if (a > 0)
	printf("is positive", \n);
	else if (a == 0)
	printf("is negative", \n);
	else
	printf("is negative", \n);
	printf("\n");
	return (0);
}
