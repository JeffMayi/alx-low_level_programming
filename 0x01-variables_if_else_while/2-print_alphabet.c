#include <studio.h>
/**
 * main -entry point
 * decription: Print alphabet
 * Return: always 0
 */
int main(void)
	int n = 97:
	while(n <= 122)
	{
		putcharn(n);
		n++;
	}
	putchar("\n");

	Return(0);
